server {
    listen 80;
    server_name quore.estate-buddy.com;

    location / {
        proxy_pass http://quore-portal:3000;
        
	proxy_http_version 1.1;
    proxy_set_header Connection "";

	proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

	# proxy_buffering off;
	# chunked_transfer_encoding off;
    }
}

server {
    listen 80;
    server_name identies.estate-buddy.com;

    location / {
        proxy_pass http://identies-portal:3000;
        
	proxy_http_version 1.1;
    proxy_set_header Connection "";

	proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

	# proxy_buffering off;
	# chunked_transfer_encoding off;
    }
}

server {
    listen 80;
    server_name custos.estate-buddy.com;

    location / {
        proxy_pass http://custos-portal:3000;
        
	proxy_http_version 1.1;
    proxy_set_header Connection "";

	proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

	# proxy_buffering off;
	# chunked_transfer_encoding off;
    }
}

server {
    listen 80;
    server_name vaulta.estate-buddy.com;

    location / {
        proxy_pass http://vaulta-portal:3000;
        
	proxy_http_version 1.1;
    proxy_set_header Connection "";

	proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

	# proxy_buffering off;
	# chunked_transfer_encoding off;
    }
}

server {
    listen 80;
    server_name linden.estate-buddy.com;

    location / {
        proxy_pass http://linden-portal:3000;
        
	proxy_http_version 1.1;
    proxy_set_header Connection "";

	proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

	# proxy_buffering off;
	# chunked_transfer_encoding off;
    }
}

server {
    listen 80;
    server_name quore-api.estate-buddy.com;

    location / {
        proxy_pass http://quore-api:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name vaulta-api.estate-buddy.com;

    location / {
        proxy_pass http://vaulta-api:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name identies-api.estate-buddy.com;

    location / {
        proxy_pass http://identies-api:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name custos-api.estate-buddy.com;

    location / {
        proxy_pass http://custos-api:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name api.estate-buddy.com;

    location / {
        proxy_pass http://linden-api:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name jobs.estate-buddy.com;

    location / {
        proxy_pass http://flower:5555;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    }
}